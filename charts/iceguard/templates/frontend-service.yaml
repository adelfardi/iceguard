apiVersion: v1
kind: Service
metadata:
  name: {{ include "iceguard.frontend.fullname" . }}
  labels:
    {{- include "iceguard.labels" (dict "ctx" . "component" "frontend") | nindent 4 }}
  {{- with (merge (dict) .Values.frontend.service.annotations .Values.commonAnnotations) }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.frontend.service.type }}
  ports:
    - name: http
      port: {{ .Values.frontend.service.port }}
      targetPort: http
      protocol: TCP
      {{- if and (eq .Values.frontend.service.type "NodePort") .Values.frontend.service.nodePort }}
      nodePort: {{ .Values.frontend.service.nodePort }}
      {{- end }}
  selector:
    {{- include "iceguard.selectorLabels" (dict "ctx" . "component" "frontend") | nindent 4 }}
